[project]
name = "batch-deserialize"
authors = ["Your Name <your.email@example.com>"]
description = "Efficient batch deserialization utility for Clarity smart contracts"
telemetry = false
requirements = []
[contracts.batch-deserialize]
path = "contracts/batch-deserialize.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
